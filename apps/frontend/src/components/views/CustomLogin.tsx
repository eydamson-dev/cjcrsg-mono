import React from 'react'
import { AdminViewProps } from 'payload'

export const CustomLogin = (props:AdminViewProps) => {
  console.log({props})
  return (
    <div>
    this is the login view
    </div>
  )
}
